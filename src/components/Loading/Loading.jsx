import React from 'react'

import "./loading.scss"

const Loading = () => {
    return (
        <div>
            <p className="saving">Loading data<span>.</span><span>.</span><span>.</span></p>
        </div>
    )
}

export default Loading
